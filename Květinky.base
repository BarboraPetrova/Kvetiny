filters:
  and:
    - file.folder == "Kvetiny/houseplants"
properties:
  note.name:
    displayName: latin
views:
  - type: cards
    name: Zobrazení
    filters:
      and:
        - owner.contains("L")
    order:
      - file.name
    image: note.image
    imageFit: contain
